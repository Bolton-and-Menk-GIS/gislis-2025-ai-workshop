import type { ArcgisExpand } from "@arcgis/map-components/components/arcgis-expand"

export const useMapWidgets = () => {

  const createExpand = (props: Partial<ArcgisExpand>) => {
    const expand = document.createElement('arcgis-expand')
  }

}